apiVersion: apps/v1
kind: Deployment
metadata:
  name: ipfs
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "nt114-fl.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ipfs
  template:
    metadata:
      labels:
        app: ipfs
    spec:
      containers:
        - name: ipfs
          image: {{ .Values.ipfs.image }}
          imagePullPolicy: {{ .Values.images.pullPolicy }}
          args: ["daemon", "--migrate=true"]
          ports:
            - containerPort: 5001
            - containerPort: 4001
          securityContext:
            runAsUser: 0
          volumeMounts:
            - name: ipfs-data
              mountPath: /data/ipfs
      volumes:
        - name: ipfs-data
{{- if .Values.storage.ipfsUsePvc }}
          persistentVolumeClaim:
            claimName: ipfs-data
{{- else }}
          emptyDir: {}
{{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: ipfs
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "nt114-fl.labels" . | nindent 4 }}
spec:
  selector:
    app: ipfs
  ports:
    - name: api
      port: 5001
      targetPort: 5001
    - name: swarm
      port: 4001
      targetPort: 4001